# Netlify configuration for frontend deployment
# This file should be in the frontend folder

[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
